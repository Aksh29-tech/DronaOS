# DronaOS

### 🗄️ Local Database Setup

This project uses Docker to run a local instance of PostgreSQL for development. Ensure you have [Docker Desktop](https://www.docker.com/products/docker-desktop/) installed and running before executing the commands below.

**To start the database in the background:**
```bash
docker compose up -d